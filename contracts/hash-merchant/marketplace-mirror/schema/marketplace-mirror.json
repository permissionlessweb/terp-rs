{
  "contract_name": "marketplace-mirror",
  "contract_version": "0.1.0",
  "idl_version": "1.0.0",
  "instantiate": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "InstantiateMsg",
    "type": "object",
    "required": [
      "accepted_denom",
      "admin"
    ],
    "properties": {
      "accepted_denom": {
        "description": "Bank denom accepted for purchases (e.g. `uterp`).",
        "type": "string"
      },
      "admin": {
        "description": "Contract admin (also protocol-fee recipient when fee is set).",
        "type": "string"
      },
      "min_buffer_blocks": {
        "description": "Settlement buffer in blocks. Default **7**. Allowed range **5–10**.",
        "default": 7,
        "type": "integer",
        "format": "uint64",
        "minimum": 0.0
      },
      "protocol_fee_bps": {
        "description": "Optional protocol fee in basis points (100 = 1%). Taken on settle.",
        "default": null,
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0.0
      }
    },
    "additionalProperties": false
  },
  "execute": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExecuteMsg",
    "oneOf": [
      {
        "description": "List a product SKU with initial stock and unit price. Caller becomes `seller`. Admin may also list.",
        "type": "object",
        "required": [
          "list_product"
        ],
        "properties": {
          "list_product": {
            "type": "object",
            "required": [
              "sku",
              "stock",
              "unit_price"
            ],
            "properties": {
              "content_cid": {
                "description": "Prefer bare sha256 hex (BUD primary). See hash-market `content-distribution.md`.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "d_tag": {
                "description": "Optional NIP-15 product d-tag for Nostr mirror.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "sku": {
                "type": "string"
              },
              "stock": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "unit_price": {
                "$ref": "#/definitions/Uint128"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Seller or admin: set absolute stock. Cannot go below reserved qty.",
        "type": "object",
        "required": [
          "update_stock"
        ],
        "properties": {
          "update_stock": {
            "type": "object",
            "required": [
              "sku",
              "stock"
            ],
            "properties": {
              "sku": {
                "type": "string"
              },
              "stock": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Buyer: pay ≥ unit_price * qty in accepted_denom. Creates a Reserved order; inventory reserved until settle/cancel.",
        "type": "object",
        "required": [
          "purchase"
        ],
        "properties": {
          "purchase": {
            "type": "object",
            "required": [
              "qty",
              "sku"
            ],
            "properties": {
              "qty": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "sku": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Anyone: settle after buffer. Pays seller (minus optional fee), decrements stock.",
        "type": "object",
        "required": [
          "settle"
        ],
        "properties": {
          "settle": {
            "type": "object",
            "required": [
              "order_id"
            ],
            "properties": {
              "order_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Buyer or admin only: cancel while Reserved; refund + release reservation. Seller cannot cancel (prevents grief during buffer escrow).",
        "type": "object",
        "required": [
          "cancel"
        ],
        "properties": {
          "cancel": {
            "type": "object",
            "required": [
              "order_id"
            ],
            "properties": {
              "order_id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "Uint128": {
        "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
        "type": "string"
      }
    }
  },
  "query": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "QueryMsg",
    "oneOf": [
      {
        "type": "object",
        "required": [
          "config"
        ],
        "properties": {
          "config": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "product"
        ],
        "properties": {
          "product": {
            "type": "object",
            "required": [
              "sku"
            ],
            "properties": {
              "sku": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "order"
        ],
        "properties": {
          "order": {
            "type": "object",
            "required": [
              "id"
            ],
            "properties": {
              "id": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "list_orders"
        ],
        "properties": {
          "list_orders": {
            "type": "object",
            "properties": {
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "reserved_qty"
        ],
        "properties": {
          "reserved_qty": {
            "type": "object",
            "required": [
              "sku"
            ],
            "properties": {
              "sku": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Last foreign inventory/oracle root stored via hashmerchant sudo.",
        "type": "object",
        "required": [
          "get_root"
        ],
        "properties": {
          "get_root": {
            "type": "object",
            "required": [
              "algo",
              "chain_uid"
            ],
            "properties": {
              "algo": {
                "type": "string"
              },
              "chain_uid": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ]
  },
  "migrate": null,
  "sudo": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "SudoMsg",
    "description": "Matches module JSON: `{\"hash_merchant\":{...}}` (loyalty-verifier / hashmerchant-test).",
    "oneOf": [
      {
        "type": "object",
        "required": [
          "hash_merchant"
        ],
        "properties": {
          "hash_merchant": {
            "type": "object",
            "required": [
              "algo",
              "chain_uid",
              "height",
              "root"
            ],
            "properties": {
              "algo": {
                "type": "string"
              },
              "attestation_count": {
                "default": 0,
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "block_time": {
                "default": 0,
                "type": "integer",
                "format": "int64"
              },
              "chain_uid": {
                "type": "string"
              },
              "height": {
                "type": "integer",
                "format": "uint64",
                "minimum": 0.0
              },
              "root": {
                "description": "Base64-encoded root bytes from the module (or passthrough hex).",
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ]
  },
  "responses": {
    "config": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ConfigResponse",
      "type": "object",
      "required": [
        "accepted_denom",
        "admin",
        "min_buffer_blocks",
        "next_order_id"
      ],
      "properties": {
        "accepted_denom": {
          "type": "string"
        },
        "admin": {
          "$ref": "#/definitions/Addr"
        },
        "min_buffer_blocks": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "next_order_id": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "protocol_fee_bps": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        }
      }
    },
    "get_root": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RootResponse",
      "type": "object",
      "required": [
        "algo",
        "attestation_count",
        "block_time",
        "chain_uid",
        "height",
        "root"
      ],
      "properties": {
        "algo": {
          "type": "string"
        },
        "attestation_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "block_time": {
          "type": "integer",
          "format": "int64"
        },
        "chain_uid": {
          "type": "string"
        },
        "height": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "root": {
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "list_orders": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrdersResponse",
      "type": "object",
      "required": [
        "orders"
      ],
      "properties": {
        "orders": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PurchaseOrder"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "OrderStatus": {
          "type": "string",
          "enum": [
            "reserved",
            "settled",
            "cancelled"
          ]
        },
        "PurchaseOrder": {
          "type": "object",
          "required": [
            "amount",
            "buyer",
            "id",
            "qty",
            "reserve_height",
            "seller",
            "settle_after",
            "sku",
            "status"
          ],
          "properties": {
            "amount": {
              "$ref": "#/definitions/Uint128"
            },
            "buyer": {
              "$ref": "#/definitions/Addr"
            },
            "id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "qty": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "reserve_height": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "seller": {
              "$ref": "#/definitions/Addr"
            },
            "settle_after": {
              "description": "`reserve_height + min_buffer_blocks` — settle only at/after this height.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "sku": {
              "type": "string"
            },
            "status": {
              "$ref": "#/definitions/OrderStatus"
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderResponse",
      "type": "object",
      "required": [
        "order"
      ],
      "properties": {
        "order": {
          "$ref": "#/definitions/PurchaseOrder"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "OrderStatus": {
          "type": "string",
          "enum": [
            "reserved",
            "settled",
            "cancelled"
          ]
        },
        "PurchaseOrder": {
          "type": "object",
          "required": [
            "amount",
            "buyer",
            "id",
            "qty",
            "reserve_height",
            "seller",
            "settle_after",
            "sku",
            "status"
          ],
          "properties": {
            "amount": {
              "$ref": "#/definitions/Uint128"
            },
            "buyer": {
              "$ref": "#/definitions/Addr"
            },
            "id": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "qty": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "reserve_height": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "seller": {
              "$ref": "#/definitions/Addr"
            },
            "settle_after": {
              "description": "`reserve_height + min_buffer_blocks` — settle only at/after this height.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "sku": {
              "type": "string"
            },
            "status": {
              "$ref": "#/definitions/OrderStatus"
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ProductResponse",
      "type": "object",
      "required": [
        "available",
        "product",
        "reserved"
      ],
      "properties": {
        "available": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "product": {
          "$ref": "#/definitions/Product"
        },
        "reserved": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Product": {
          "type": "object",
          "required": [
            "seller",
            "sku",
            "stock",
            "unit_price"
          ],
          "properties": {
            "content_cid": {
              "description": "Prefer bare sha256 hex (BUD / content-plane primary).",
              "type": [
                "string",
                "null"
              ]
            },
            "d_tag": {
              "description": "Optional NIP-15 product d-tag.",
              "type": [
                "string",
                "null"
              ]
            },
            "seller": {
              "$ref": "#/definitions/Addr"
            },
            "sku": {
              "type": "string"
            },
            "stock": {
              "description": "Absolute listed stock (not reduced on reserve; reduced on settle).",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "unit_price": {
              "$ref": "#/definitions/Uint128"
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "reserved_qty": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ReservedQtyResponse",
      "type": "object",
      "required": [
        "reserved",
        "sku"
      ],
      "properties": {
        "reserved": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "sku": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
