# DEPRECATED — replaced by tiered CI.
# See: ci-core.yml (always), ci-extended.yml (paths/labels), ci-heavy.yml (admin/nightly).
# This stub keeps history; job always succeeds with a pointer so old branch protection
# rules referencing "Basic" do not silently mean nothing forever.
name: Basic (deprecated)

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redirect:
    name: use CI Core instead
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::warning::Workflow 'Basic' is deprecated. Real gates are CI Core / CI Extended / CI Heavy."
          echo "See docs/ci.md and .github/workflows/ci-*.yml"
