# cw-headstash contract

| | |
|--|--|
| **Category** | `contracts-circuit` |
| **Monorepo path** | `crates/headstash/contracts/cw-headstash/README.md` |
| **Role** | Mint router + BridgeMintNote; wasmd-safe guest build |
| **Non-goals** | Does not own observe plane or UI; host floor is platform-wasmvm |

> Thin shell only. Source of truth is the document below (synced into `src/_include/` on build). Do not edit the include tree by hand.

{{#include ../_include/crates/headstash/contracts/cw-headstash/README.md}}
