# seam_note_out

Pure L0 fixture: structural `SeamNoteOutV0` (SEAM-NOTE-OUT.md) + encrypt envelope + optional notes HTTP client. Bin: `headstash-notes`.

```bash
cd docs/plans/spectrum/fixtures/seam_note_out && cargo test
```

**Tests:** SEAM-N1 … SEAM-N6. Features: `http`, `auth` for hash-market note persistence helpers.
