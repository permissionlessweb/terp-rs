# bridge_auth_seams

Pure L0 fixture: Domain B bridge-mint policy + Tacit↔Terp hinge (`authorize_bridge_mint` → SEAM-NOTE-OUT sketch). No SP1/halo2.

```bash
cd docs/plans/spectrum/fixtures/bridge_auth_seams && cargo test
```

**Seeds:** E2E-01..07 (H-1 burn≠spend, conservation, destCommitment, once-per-ν).
