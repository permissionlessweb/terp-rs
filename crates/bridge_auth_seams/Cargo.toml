[package]
name = "bridge_auth_seams"
version = "0.1.0"
edition = "2021"
description = "Domain B pure seams + Tacit↔Terp bridge hinge (H-1 burn≠spend, authorize_bridge_mint, SEAM-NOTE-OUT sketch)"
publish = false

[dependencies]
sha2 = "0.10"

[lib]
path = "src/lib.rs"
